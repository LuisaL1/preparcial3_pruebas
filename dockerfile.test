FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
COPY tests/ ./tests/

ENV DATABASE_URL="postgresql://test_user:test_pass@db-test:5432/test_db"
ENV PYTHONPATH="/app"

CMD ["pytest", "tests/", "-v", "--tb=short"]